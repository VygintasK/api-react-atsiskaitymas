import React from 'react'

const Country = ({name}) => {

  return (
    <div className='country-wrap'>
        <h1>{name}</h1>
        <h1>{name}</h1>
        <h1>{name}</h1>
        <h1>{name}</h1>
        <h1>{name}</h1>
    </div>
  )
}

export default Country